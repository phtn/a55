import { IconName } from '../icons'
import { Token } from './token-coaster'

export const tokenIcons: Record<Token, IconName> = {
  usdc: 'usdc',
  ethereum: 'eth',
  bitcoin: 'btc',
  matic: 'pol',
  usdt: 'usdt',
  pol: 'pol',
  sepolia: 'eth'
}
